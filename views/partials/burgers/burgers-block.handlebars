<li>
	{{burgername}}

	{{#if devoured}}
		<button class="change-devoured" data-id="{{id}}" data-newburger="{{devoured}}">	EAT ME ! </button>
	{{!-- {{else}}REVIVE ME ! --}}
	{{/if}}
		
	{{#if devoured}} 
	{{else}}
		<button class="delete-burger" data-id="{{this.id}}"> DELETE!</button>
	{{/if}}

</li>
